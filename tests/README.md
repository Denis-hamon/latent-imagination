# Tests

Top-level test suites: e2e, determinism replays, and the CI guard suite (`guards/`). Semantics of each guard are documented here as they land (writer inventory, import-lint, dependency-scan, network sandbox — Story 1.5).
