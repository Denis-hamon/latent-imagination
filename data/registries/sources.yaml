# Seed registry (FR-1). These three seed entries are fabrications for Story 1.7
# fixtures — real registrations happen when sources are actually ingested (1.9+),
# with snapshot hashes computed from real content.

sources:
  - source_id: own-harbor-seed
    origin_class: own_harbor_run
    license: "ours"
    usage_rights: "full"
    covered_families: ["claude-code"]
    covered_generations: ["2026"]
    snapshot_content_hash: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
    coverage_gaps: ["pre-2026 generations — archives only"]
    source_version: 1
    registered_at: "2026-08-05T00:00:00Z"
    notes: "seed entry for Story 1.7"

  - source_id: public-traject-seed
    origin_class: public_trajectory_collection
    license: "MIT"
    usage_rights: "analysis + derivative labels"
    covered_families: ["swe-agent", "openhands"]
    covered_generations: ["2024", "2025"]
    snapshot_content_hash: "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"
    coverage_gaps: []
    source_version: 1
    registered_at: "2026-08-05T00:00:00Z"
    notes: "seed entry for Story 1.7"

  # REAL entry (story 4.1): replaces the Story-1.7 `public-ci-seed` fixture.
  # snapshot hash = sha256("[]") = the empty canonical set (store bootstrap
  # convention, store/layout.EMPTY_STORE_VERSION): pre-first-harvest state.
  # The first real harvest run mutates the content -> audit_source() bumps
  # source_version to 2 and the new snapshot hash is recorded then.
  - source_id: github-actions-public-ci
    origin_class: public_ci_logs
    license: "per-repo SPDX id recorded per item (policy allowlist: MIT, Apache-2.0, BSD-2/3-Clause, ISC, 0BSD)"
    usage_rights: "analysis + derivative labels; patches republished only from allowlisted-license repos"
    covered_families: ["ci-generic"]
    covered_generations: ["2023", "2024", "2025", "2026"]
    snapshot_content_hash: "4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945"
    coverage_gaps: ["pre-GitHub-Actions CI estates", "patch-level F2P linkage is the Clean Tier's job, not this source's"]
    source_version: 1
    registered_at: "2026-08-06T00:00:00Z"
    notes: "harvest governed by governance/corpus/harvest-policy-v1.toml (pre-registered, story 4.1)"

  # REAL entry (story 14.1, 2026-08-16) : première source own-repo TS à signal
  # de test exécutable (vitest). Pilote F2P/P2P prouvé bout-en-bout, zéro juge
  # (bug sémantique -> tests nommés échouent -> gold reverdit). snapshot = le
  # pilot-task scellé.
  - source_id: own-acre-blocks-ts
    origin_class: own_repo_mutant_tasks
    license: "ours"
    usage_rights: "full"
    covered_families: ["acre__blocks"]
    covered_generations: ["2026"]
    snapshot_content_hash: "0215a8fb773c8e844046c28aedda667c218b7b950d4fa9e927a938cb2e8a773a"
    coverage_gaps: ["un seul module prouvé (diff3-merge) ; l'extension à d'autres modules/packages acre est une fenêtre à part"]
    source_version: 1
    registered_at: "2026-08-16T14:00:00Z"
    notes: "vitest 4.1.10 ; repo = Acre worktree wt-20-13 (packages/blocks) ; mutant protocole = swe-smith répliqué TS ; FR-3 labels re-derivables des traces vitest"

  # REAL entry (story 14/E14-v2, 2026-08-16) : monorepo Astro/TS own-infra
  # (serveur Kimsufi-standard), 53 specs vitest / 1663 tests verts, pilote
  # F2P/P2P prouvé remote zéro-appel. Mesure LOCALE seulement — aucune
  # republication de contenu (marque tierce), les labels ne citent que les
  # traces vitest.
  - source_id: own-kimsufi-site-ts
    origin_class: own_repo_mutant_tasks
    license: "ours"
    usage_rights: "full (measurement local; no republication of branded content)"
    covered_families: ["kimsufi__site"]
    covered_generations: ["2026"]
    snapshot_content_hash: "e1df27a5dc274c1cc297ada9e2b4d823f0e35123e5408d785d266845dd7bb069"
    coverage_gaps: ["1 fichier spec exclu (translations-setup-fee : data drift i18n)", "mutants restants à générer (fenêtre v2)"]
    source_version: 1
    registered_at: "2026-08-16T14:30:00Z"
    notes: "vitest 4.1.8 remote (serveur Kimsufi-standard) ; le labeling de la fenêtre v2 s'exécutera sur ce serveur (chaîne distante sérialisée) ; modules cibles prioritaires : content-adapters (DrupalComponentRenderer 43 tests, transformers.slider 28, buildComponentsFromContent 15, normalize-content 14)"

  # CANDIDATE source (2026-08-16, découverte owner) : worldmonitor (GitHub
  # koala73/worldmonitor @ ef9c8e652ef3) — 1378 fichiers de tests
  # (node:test/tsx + vitest convex/dom + playwright e2e) validant la logique
  # applicative réelle (rate-limiters à état, contrats feeds, import-graphs
  # cross-modules). AGPL-3.0-only : analyse + labels dérivés AUTORISÉS
  # (précédent ci-logs), republication des patches INTERDITE → les diffs ne
  # vivent qu'en mesure interne, jamais dans un artefact public. Non utilisée
  # tant qu'une fenêtre dédiée n'est pas pré-enregistrée (v3).
  - source_id: public-worldmonitor-ts
    origin_class: public_trajectory_collection
    license: "AGPL-3.0-only"
    usage_rights: "analysis + derivative labels internal only; no patch republication"
    covered_families: ["worldmonitor__app"]
    covered_generations: ["2026"]
    snapshot_content_hash: "070a92871ac9ac61d6bbf16f790bb6516b493acfa104a4cc3af41cdaaac21736"  # pré-ingestion : sha256("worldmonitor@ef9c8e652ef3…") ;
    # remplacé par le sha256 du contenu ingéré à la première fenêtre (précédent ci-seed)
    coverage_gaps: ["jamais utilisée en fenêtre ; installation node + sélection des suites exécutables à valider"]
    source_version: 1
    registered_at: "2026-08-16T15:00:00Z"
    notes: "commit figé à l'enregistrement ; classe de tests plus dure que mutants mono-fonction (état, multi-fichiers) — candidat naturel pour obtenir des négatifs groundés"

  # REAL entry (pilote 2026-08-16) : OmniRoute release/v3.8.50 — MIT, 4384 tests
  # unitaires numérotés par issue (bug-fix réels + non-régression), runner
  # node:test via tsx, validé vert sur Kimsufi-standard ; pilote chaîne F2P/P2P
  # + sonde auteur bi-étage OK (easy 2/2 réparés, triple 0/1).
  - source_id: public-omniroute-ts
    origin_class: own_repo_mutant_tasks
    license: "MIT"
    usage_rights: "full (allowlist licences)"
    covered_families: ["omniroute__app"]
    covered_generations: ["2026"]
    snapshot_content_hash: "8c5ef7c75a456ad3bec211bc4379dba48df0836169798a6d2cff224fab259316"
    coverage_gaps: ["pilote sur 1 module (compression/lite) ; l'extension du catalogue mutants aux autres modules se fait à la construction du quota"]
    source_version: 1
    registered_at: "2026-08-16T17:00:00Z"
    notes: "commit figé e646fe84c76d ; tests exécutés sur Kimsufi-standard (npm install fait, package-lock restauré post-install) ; classe de difficulté validée par sonde : mutants easy réparés, triples coordonnés échoués"

  # REAL entry (2026-08-16, autonomie owner option géométrie/échelle) :
  # zod (colinhacks/zod @ 5e608851) — MIT confirmé (packages/zod/LICENSE),
  # monorepo pnpm, vitest ; runner validé sur Kimsufi-standard
  # (packages/zod/src/v4/classic/tests/string.test.ts : 94 tests verts, ~7.5 s).
  # Logique applicative pure densément testée (validations, coercions,
  # comparaisons numériques/dates/enums) — candidat naturel pour mutants
  # doubles négatifs-first et familles Mondrian nombreuses.
  - source_id: public-zod-ts
    origin_class: own_repo_mutant_tasks
    license: "MIT"
    usage_rights: "full (allowlist licences)"
    covered_families: ["zod__v4"]
    covered_generations: ["2026"]
    snapshot_content_hash: "64e4a1210548121c4ae8611891fc49ea87eebf2d190a7cb3fefd1075cc7e1072"  # pré-ingestion : sha256("zod@<commit>") ;
    # remplacé par le sha256 du contenu ingéré à la première fenêtre (précédent ci-seed)
    coverage_gaps: ["aucun mutant construit ni validé à ce jour ; suite vitest complète non encore exécutée (1 fichier validé en pilote)"]
    source_version: 1
    registered_at: "2026-08-16T15:45:00Z"
    notes: "commit figé 5e608851fbc7 à l'enregistrement ; clone Kimsufi-standard:~/zod-source ; pnpm install OK ; tests = packages/zod/src/v4/{classic,core,mini}/tests ; runner: npx vitest run <file>"

  # REAL entry (2026-08-16, autonomie owner — diversification sources) :
  # date-fns (date-fns/date-fns @ a0a39220) — MIT confirmé (pkgs/core/LICENSE.md),
  # monorepo pnpm ; runner validé sur Kimsufi-standard
  # (pkgs/core : npx vitest run src/addDays/test.ts, 9 passed, 173 ms).
  # Intérêt structurel : ~350 fonctions pures chacune avec son propre test.ts =>
  # families naturelles nombreuses et fines (date_fns__addDays, ...) pour strates
  # Mondrian ; logique numérique/frontières temporelles = mutations doubles propres.
  - source_id: public-date-fns-ts
    origin_class: own_repo_mutant_tasks
    license: "MIT"
    usage_rights: "full (allowlist licences)"
    covered_families: ["date_fns__core"]
    covered_generations: ["2026"]
    snapshot_content_hash: "fc9cc6f661e0905d49a74da267beb929c4d1c4b1fca536de70278d3b044e5e42"  # pré-ingestion : sha256("date-fns@<commit>")
    coverage_gaps: ["aucun mutant construit ni validé à ce jour ; pilote = 1 fichier test seulement"]
    source_version: 1
    registered_at: "2026-08-16T15:50:00Z"
    notes: "commit figé a0a3922052 à l'enregistrement ; clone Kimsufi-standard:~/date-fns-source ; tests exécutés DEPUIS pkgs/core (racine du monorepo ne matche rien) ; runner: cd pkgs/core && npx vitest run src/<fn>/test.ts"
